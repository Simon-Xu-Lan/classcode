
// module.exports is an object we use to store variables or methods
module.exports = {
    sum: function(x, y) { return x + y},
    difference: function(x, y) {return x - y},
    product: function(x, y) {return x * y},
    quotient: function(x, y) {return x / y}
};